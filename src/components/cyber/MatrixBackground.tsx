/**
 * CyberBackground — Retro starfield canvas background.
 * Replaces the old dot-grid + scanline overlay with a
 * subtle twinkling star canvas.
 */
import { Starfield } from "./Starfield";

export function CyberBackground() {
  return <Starfield />;
}
